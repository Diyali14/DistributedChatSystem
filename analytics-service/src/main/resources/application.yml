server:
  port: 8088

spring:
  application:
    name: analytics-service
  config:
    import: "optional:configserver:http://config-server:8888"

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
